# Supabase (optional — the app runs fully in demo mode without these)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI (optional — without a key, QANUNI uses its local heuristic AI engine)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
